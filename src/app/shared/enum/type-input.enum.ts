import {Observable} from "rxjs";

export enum TypeInput {
  Input = "input",
  Picker = "picker",
  Select = "select",
  Observable = "observable",
  Number = "number",
  CheckBox = "checkbox",
  Radio = "radio"
}
